import type { Metadata } from "next";
import "./globals.css";
import Header from "@/components/Header";
import Footer from "@/components/Footer";

export const metadata: Metadata = {
  title: "MemeForge - Free Crypto Meme Generator",
  description: "Create amazing cartoon crypto memes for free. No complex tools, just pure meme magic for the community.",
  keywords: "crypto memes, meme generator, cartoon generator, crypto art, free meme maker",
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body className="antialiased">
        <Header />
        <main className="min-h-screen">
          {children}
        </main>
        <Footer />
      </body>
    </html>
  );
}
